version: "2"

linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
  exclusions:
    paths:
      - internal/db/gen/
      - internal/web/api/gen/
      - web/node_modules/

formatters:
  enable:
    - gofmt
